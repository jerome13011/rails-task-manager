<% @tasks.each do |task| %>
  <h2><%= task.name %></h2>
  <p><%= task.description %></p>
  <p><%= task.done %></p>
  <p><%= link_to task.name, task_path(task) %></p>
<% end %>

<%= link_to "creer", new_task_path %>



